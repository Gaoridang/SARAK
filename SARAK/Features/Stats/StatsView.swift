// StatsView.swift — SARAK
import SwiftUI

struct StatsView: View {
    var body: some View {
        NavigationStack {
            Text("통계")
                .navigationTitle(StringConstants.Tab.stats)
        }
    }
}
