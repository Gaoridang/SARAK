# Sync Quick Guide

- Offline-first: local-first UX, remote sync in background.
- Never block UI on sync.
- Queue and retry remote writes safely.
- Surface safe user-visible status/errors from ViewModel.

Open full `.harness/sync.md` only when changing conflict/retry semantics or sync-trigger behavior.
